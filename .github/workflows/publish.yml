name: Publish

on:
  push:
    tags:
      - 'v*'

jobs:
  test:
    uses: contact-api/.github/.github/workflows/test.yml@v1.1.0

  publish:
    needs: test
    uses: contact-api/.github/.github/workflows/publish.yml@v1.1.0
    secrets: inherit
